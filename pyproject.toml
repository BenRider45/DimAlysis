[project]
name = "dimalysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ben Rider", email = "ben.rider-1@colorado.edu" }
]
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "typer>=0.27.2",
]

[project.scripts]
dimalysis = "dimalysis.cli:app"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
